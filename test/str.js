'use strict'

//----------------------------------------------------------
// modules
//----------------------------------------------------------
// npm
const t = require('chai').assert

// local
const str = require('../lib/str')

//----------------------------------------------------------
// tests
//----------------------------------------------------------
describe('str', () => {
  it('longest', () => {
    t.equal(str.longest(['one', 'two', 'three', 'four']), 5)
  })
})
