'use strict'

//----------------------------------------------------------
// modules
//----------------------------------------------------------
// npm
const t = require('chai').assert

// local
const time = require('../lib/time')

//----------------------------------------------------------
// tests
//----------------------------------------------------------
describe('time', () => {
  it('digit', () => {
    t.equal(time.digit(10), 10)
    t.equal(time.digit(5), '05')
  })
})
